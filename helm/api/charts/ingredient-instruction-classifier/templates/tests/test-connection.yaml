apiVersion: v1
kind: Pod
metadata:
  name: rs-ingredient-instruction-classifier-test-connection
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['rs-ingredient-instruction-classifier:80']
  restartPolicy: Never
