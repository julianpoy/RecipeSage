{
  "name": "cli",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/cli/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": [
        "{options.outputPath}"
      ],
      "defaultConfiguration": "production",
      "dependsOn": [
        "util-shared:build",
        "util-server:build"
      ],
      "options": {
        "platform": "node",
        "outputPath": "dist/apps/cli",
        "format": [
          "cjs"
        ],
        "bundle": true,
        "minify": false,
        "external": [
          "@sentry/profiling-node",
          "jsdom",
          "pdfmake",
          "yauzl-promise"
        ],
        "main": "packages/cli/src/main.ts",
        "tsConfig": "packages/cli/tsconfig.json",
        "generatePackageJson": true,
        "esbuildConfig": "packages/cli/esbuild.config.js"
      },
      "configurations": {
        "development": {
          "skipTypeCheck": true,
          "watch": true
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "cli:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "cli:build:development"
        },
        "production": {
          "buildTarget": "cli:build:production"
        }
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/cli",
        "commands": [
          "tsc --project tsconfig.app.json --noEmit",
          "tsc --project tsconfig.spec.json --noEmit"
        ],
        "parallel": true
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "maxWarnings": 0
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": [
        "{options.reportsDirectory}"
      ],
      "options": {
        "reportsDirectory": "{projectRoot}/../../coverage/packages/cli"
      }
    }
  }
}
