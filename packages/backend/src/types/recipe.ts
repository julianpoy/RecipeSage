/**
 * Incomplete recipe model
 */
export interface Recipe {
  id: string;
  userId: string;
  title: string;
  source: string;
  description: string;
  ingredients: string;
  instructions: string;
  notes: string;
}

